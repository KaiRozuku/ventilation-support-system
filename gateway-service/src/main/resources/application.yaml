server:
  port: 8082

spring:
  application:
    name: gateway-service
  config:
    import: optional:file:.env[.properties]

security:
  excluded-urls:
  - /auth/login/**
  - /auth/register/**
  - /auth/refreshToken/**
  secret-key: ${JWT_SECRET_KEY}