server:
  port: 8082


spring:
  application:
    name: gateway-service
  config:
    import: optional:file:.env[.properties]

security:
  excluded-urls:
  - /login/**
  - /register/**
  - /refreshToken/**
  secret-key: ${JWT_SECRET_KEY}