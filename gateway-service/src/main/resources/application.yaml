server:
  port: 8082

spring:
  application:
    name: gateway-service
  config:
    import: optional:file:.env[.properties]

jwt:
  secret-key: ${JWT_SECRET_KEY}

security:
  excluded-urls:
    - /auth/login
    - /auth/register
    - /auth/refresh